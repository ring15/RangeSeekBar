<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <com.founq.sdk.rangeseekbar.widget.RangeBar
        android:id="@+id/range_bar"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        app:mode="rangeBar"
        app:rangePosition="up"
        app:imgType="img"
        app:rangeSelectedColor="@color/selectLineColor"
        app:selectLineColor="@color/selectLineColor"
        app:disableColor="@color/selectLineColor"
        app:normalColor="@color/colorPrimary"
        app:pressedColor="@color/colorAccent"
        app:rangeHeight="4dp"/>

</LinearLayout>